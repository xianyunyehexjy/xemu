Source: xemu
Section: games
Priority: optional
Maintainer: Matt Borgerson <contact@mborgerson.com>
Build-Depends: debhelper (>= 11),
# In comments below we also specify (system-specific) arguments
# to qemu's configure script, -- optional features which depend
# on build-dependencies.
# pc-bios/*.dts => *.dtb (PPC firmware)
# device-tree-compiler,
 python3:any,
# --enable-docs
# texinfo, python3-sphinx,
# iasl (from acpica-tools) is used only in a single test these days, not for building
# acpica-tools,
# --enable-capstone=system
# libcapstone-dev,
# --enable-linux-aio	linux-*
# libaio-dev [linux-any],
# --audio-drv-list=pa,alsa,oss	linux-*
# --audio-drv-list=pa,oss	kfreebsd-*
# libpulse-dev,
# libasound2-dev [linux-any],
# for virtfs (now in libc6)
# --enable-attr
# --enable-brlapi
# libbrlapi-dev,
# --enable-virtfs	linux-*
# needed for virtfs
# libcap-dev [linux-any],
# --enable-cap-ng	linux-*
# libcap-ng-dev [linux-any],
# --enable-curl
# libcurl4-gnutls-dev,
# --enable-fdt
# libfdt-dev,
# --enable-gnutls
# gnutls-dev,
# --enable-gtk --enable-vte
# libgtk-3-dev, libvte-2.91-dev,
 libgtk-3-dev,
# --enable-libiscsi
# libiscsi-dev (>> 1.9.0~),
# --enable-curses
# libncursesw5-dev,
# --enable-virglrenderer	linux-*
# libvirglrenderer-dev [linux-any],
# --enable-opengl
# libepoxy-dev, libdrm-dev, libgbm-dev,
 libepoxy-dev,
:debian:# --enable-libnfs
:debian: libnfs-dev (>> 1.9.3),
# --enable-numa	i386|amd64|ia64|mips|mipsel|powerpc|powerpcspe|x32|ppc64|ppc64el|arm64|sparc|s390x|riscv64
# libnuma-dev   [i386 amd64 ia64 mips mipsel mips64 mips64el powerpc powerpcspe x32 ppc64 ppc64el arm64 sparc s390x riscv64],
# --enable-smartcard
# libcacard-dev,
 libpixman-1-dev,
# --enable-rbd		amd64|arm64|armel|armhf|i386|mips64el|mipsel|ppc64el|s390x|ppc64|riscv64|sparc64
# librbd-dev            [amd64 arm64 armel armhf i386 mips64el mipsel ppc64el s390x ppc64 riscv64 sparc64],
# glusterfs is debian-only since ubuntu/glusterfs is in universe (MIR LP: #1274247)
# before buster it was glusterfs-common so keep it for now for bpo
:debian:# --enable-glusterfs	linux-any
:debian: libglusterfs-dev [linux-any] | glusterfs-common [linux-any],
# --enable-vnc-sasl
# libsasl2-dev,
# --disable-sdl
 libsdl2-dev,
# --enable-seccomp	amd64|arm64|armel|armhf|i386|mips64el|mipsel|ppc64el|s390x|hppa|powerpc|ppc64|riscv64|x32
# libseccomp-dev        [amd64 arm64 armel armhf i386 mips64el mipsel ppc64el s390x hppa powerpc ppc64 riscv64 x32],
# --enable-slirp=system
# libslirp-dev,
# --enable-spice	linux-amd64|linux-i386|linux-arm64
# libspice-server-dev (>= 0.12.5~) [linux-amd64 linux-i386 linux-arm64],
# libspice-protocol-dev (>= 0.12.3~) [linux-amd64 linux-i386 linux-arm64],
# --enable-rdma
# librdmacm-dev, libibverbs-dev, libibumad-dev,
# --enable-linux-io-uring	linux-*
# liburing-dev [linux-any],
# --enable-libusb	linux-*
# libusb-1.0-0-dev (>= 2:1.0.13~) [linux-any],
# --enable-usb-redir	linux-*
# libusbredirparser-dev (>= 0.6~) [linux-any],
# --enable-libssh
# libssh-dev,
# vde is debian-only since ubuntu/vde2 is in universe
:debian:# --enable-vde
:debian: libvdeplug-dev,
# --enable-xen	linux-amd64|linux-i386
# libxen-dev [linux-amd64 linux-i386],
# --enable-nettle
# nettle-dev,
## always enabled: --enable-uuid
# uuid-dev,
# --enable-xfsctl	linux-*
# xfslibs-dev [linux-any],
# always needed
 zlib1g-dev,
# other optional features we enable
# no configure flag, needed for qga
# libudev-dev,
# --enable-vnc
# --enable-vnc-jpeg
# libjpeg-dev,
# --enable-vnc-png
# libpng-dev,
# --enable-libpmem	linux-amd64|linux-arm64|linux-ppc64el
# libpmem-dev [linux-amd64 linux-arm64 linux-ppc64el],
# --enable-kvm		linux-*
# --enable-vhost-net	linux-*	# is it really linux-specific?
##--enable-lzo todo, for (memory) dumps
##--enable-netmap todo bsd
##--enable-xen-pci-passthrough todo
##--with-iconv (libiconv for curses wide char support)
## libudev?
## auth-pam - for auth for vnc&Co using PAM
## gio-2.0 - for -display=spice-app
 libssl-dev,
#Build-Depends-Indep:
# gcc-s390x-linux-gnu,
# libc6.1-dev-alpha-cross isn't really needed but the code,
# for whatever reason, #include's string.h which needs libc6.1-dev
# gcc-alpha-linux-gnu, libc6.1-dev-alpha-cross,
# skiboot firmware, openbios
# gcc-powerpc64-linux-gnu,
# openbios
# gcc-sparc64-linux-gnu, fcode-utils, xsltproc,
# hppa-firmware
# gcc-hppa-linux-gnu,
# opensbi
# gcc-riscv64-linux-gnu,
#Build-Conflicts: oss4-dev
Standards-Version: 3.9.8
Homepage: https://xemu.app
:debian:Vcs-Browser: https://github.com/mborgerson/xemu
:debian:Vcs-Git: https://github.com/mborgerson/xemu.git
:ubuntu:XS-Debian-Vcs-Browser: https://github.com/mborgerson/xemu
:ubuntu:XS-Debian-Vcs-Git: https://github.com/mborgerson/xemu.git
:ubuntu:Vcs-Browser: https://github.com/mborgerson/xemu
:ubuntu:Vcs-Git: https://github.com/mborgerson/xemu.git

Package: xemu
Architecture: amd64 arm arm64 armel armhf i386 ia64 kfreebsd-amd64 kfreebsd-i386 mips mipsel mips64 mips64el powerpc powerpcspe ppc64 ppc64el riscv64 s390x sparc sparc64 x32
Multi-Arch: foreign
Depends: ${shlibs:Depends}, ${misc:Depends}
:ubuntu: cpu-checker
Provides: xemu
Description: Original Xbox emulator
 An open-source, cross-platform application that emulates the hardware of the
 original Xbox game console.
